<div class="container">
    <div class="col-sm-6 col-sm-offset-3"><hr>

        <!-- show any messages that come back with authentication -->
         {{!-- {{>if (message.length > 0) { }} --}}
        {{!-- {{/if}} --}}
        

        <!-- LOGIN FORM -->
        <div class="row">
            <a method="POST" class="fa fa-twitter fa-5x" aria-hidden="true" href="/login/twitter"></a>
            <a method="POST" class="fa fa-facebook fa-5x" aria-hidden="true" href="/auth/facebook"></a>
            <a method="POST" class="fa fa-reddit fa-5x" aria-hidden="true" href="/auth/reddit"></a>
            <a method="POST" class="fa fa-google fa-5x" aria-hidden="true" href=""></a>
        </div>
        {{!-- <form action="/login" method="post">
            <div class="form-group">
                <label>Email</label>
                <input type="text" class="form-control" name="email">
            </div>
            <div class="form-group">
                <label>Password</label>
                <input type="password" class="form-control" name="password">
            </div>

            <button type="submit" class="btn btn-warning btn-lg">Login</button>
        </form> --}}
        
        <hr>
        <p>Need an account? <a href="/signup">Signup</a></p>
        <p>Or go <a href="/">home</a>.</p>
    </div>
</div>